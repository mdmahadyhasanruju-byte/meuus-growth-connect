# Public production Chat Trigger webhook only. Do not place editor, API, OAuth, model, or Google credentials here.
# Keep empty until owner approval, public version alignment, allowed-origin restriction, and workflow publication.
VITE_N8N_SOULAI_WEBHOOK_URL=
