# Codex Preflight Checklist — TanStack Start V1

Hard gate.

Phase 0 must inspect:
- Git status and uncommitted work
- all governance documents
- bun.lock and package.json
- TanStack Start/Router versions
- src/routes, src/router.ts, src/routeTree.gen.ts, src/start.ts, src/server.ts
- existing route conventions and generated-route workflow
- current types, reducers, storage, hydration, tests, and styles
- files and routes that must be preserved

First slice only:
/journey → boundary → preferences → orientation → KBA-LQ-001 → explicit private reflection resolution → internal non-terminal checkpoint

STOP after the first slice. Continue only after the full checkpoint report, runtime audit, no drift flags, and explicit approval.

Dual hydration proof:
- cold: localStorage restored
- warm: memory overrides stale localStorage

Negative proofs:
- milestone not created
- private text not persisted in progress_only
- invalid route inaccessible
- no direct UI/route mutation
- no external private transmission
